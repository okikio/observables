# Changelog

All notable changes to `@okikio/observables` will be documented in this file.

Releases are generated from Conventional Commits by the `Release` workflow, so
each published version updates this changelog and the `version` field in
`deno.jsonc` together.
